- if user_signed_in?
  .ui.page.grid
    .one.column.row
      = render 'layouts/messages'

      #timeline
        - @timeline.each do |answer|
          = render 'shared/answerbox', a: answer

- else
  = render 'static/front'
